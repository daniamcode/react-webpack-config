import React from 'react'
import ReactDOM from 'react-dom'

// export const secondVersion = () => {
//     document.getElementById("app").innerText = "Second version!!"
// };

// secondVersion();

// to consider optional chaining
const obj = {}
obj?.hi

ReactDOM.render(
    <h3>Third version, adding React!!</h3>, document.getElementById('app')
)

